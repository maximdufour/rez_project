@page
@model WebApplication.Pages.LesTransactionsArgent.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TransactionArgent[0].Montant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TransactionArgent[0].JoueurNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TransactionArgent[0].RepasNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TransactionArgent) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Montant)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JoueurNavigation.Nom)
            </td>
            <td>
                @{ var identity = $"{item.RepasNavigation.DateRepas} | {item.RepasNavigation.Repas1Navigation.Nom}"; }
                @Html.DisplayFor(modelItem => identity)
            </td> 
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
